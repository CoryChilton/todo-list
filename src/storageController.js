import { logicController } from "./logicController";

export const storageController = (function() {
  // const projects = document.getElementById('projects');
  // const projectChange = () => {
  //   console.log('project change');
  // };
  // const projectObserver = new MutationObserver(projectChange);
  // projectObserver.observe(projects, { childList: true });

  // const todos = document.getElementById('todos');
  // const todoChange = () => {
  //   console.log('todo change');
  // };
  // const todoObserver = new MutationObserver(todoChange);
  // todoObserver.observe(todos, { childList: true })

  


})();